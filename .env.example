# The Tal Family OS — environment template
# No variables are required in the current mock-data phase.
# The future Supabase phase will use (names subject to final integration):
#
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
# SUPABASE_SERVICE_ROLE_KEY=        # server-only, never exposed to the browser
#
# Never commit real values. Copy to .env.local for local development.
