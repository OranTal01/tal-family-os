import { PageContainer, PageHeader } from '@/components/shell/page-container';

export const metadata = { title: 'תקציב חודשי' };

export default function Page() {
  return (
    <PageContainer>
      <PageHeader title='תקציב חודשי' />
    </PageContainer>
  );
}
