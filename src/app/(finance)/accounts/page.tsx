import { PageContainer, PageHeader } from '@/components/shell/page-container';

export const metadata = { title: 'חשבונות וכרטיסים' };

export default function Page() {
  return (
    <PageContainer>
      <PageHeader title='חשבונות וכרטיסים' />
    </PageContainer>
  );
}
