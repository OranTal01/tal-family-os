import { PageContainer, PageHeader } from '@/components/shell/page-container';

export const metadata = { title: 'בית מול עסק' };

export default function Page() {
  return (
    <PageContainer>
      <PageHeader title='בית מול עסק' />
    </PageContainer>
  );
}
