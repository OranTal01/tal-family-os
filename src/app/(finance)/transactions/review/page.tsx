import { PageContainer, PageHeader } from '@/components/shell/page-container';

export const metadata = { title: 'תנועות לבדיקה' };

export default function Page() {
  return (
    <PageContainer>
      <PageHeader title='תנועות לבדיקה' />
    </PageContainer>
  );
}
