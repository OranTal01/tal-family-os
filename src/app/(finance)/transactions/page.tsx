import { PageContainer, PageHeader } from '@/components/shell/page-container';

export const metadata = { title: 'תנועות' };

export default function Page() {
  return (
    <PageContainer>
      <PageHeader title='תנועות' />
    </PageContainer>
  );
}
