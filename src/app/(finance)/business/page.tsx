import { PageContainer, PageHeader } from '@/components/shell/page-container';

export const metadata = { title: 'מבט עסק דניאל' };

export default function Page() {
  return (
    <PageContainer>
      <PageHeader title='מבט עסק דניאל' />
    </PageContainer>
  );
}
